server:
  port: 0
spring:
  datasource:
    url: jdbc:postgresql://${yangxj96.wsl}:5432/postgres
  cloud:
    loadbalancer:
      cache:
        enabled: true
        caffeine:
          spec: initialCapacity=500,expireAfterWrite=5s
# mybatis plus 删除
mybatis-plus:
  type-aliases-package: io.github.yangxj96.bean.user
yangxj96:
  security:
    enable: true
    store-type: redis
