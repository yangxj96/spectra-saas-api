server:
  port: 0
spring:
  datasource:
    url: jdbc:postgresql://${yangxj96.wsl}:5432/yangxj96_saas_db
  rabbitmq:
    host: ${yangxj96.wsl}
    username: root
    password: QuVsKppcWvwwX2Vv
    virtual-host: /
  cloud:
    stream:
      rabbit:
        bindings:
          tenant-in-0:
            consumer:
              acknowledge-mode: MANUAL
              anonymous-group-prefix: yangxj96.serve.auth.
      binders:
        default:
          type: rabbit
      bindings:
        tenant-in-0:
          destination: saas.tenant
          group: ${spring.application.name}

mybatis-plus:
  type-aliases-package: io.github.yangxj96.bean.user
yangxj96:
  security:
    enable: false
    store-type: redis
  db:
    security-db: 9
